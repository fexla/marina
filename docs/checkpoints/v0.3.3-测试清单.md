# v0.3.3 测试清单

> 测试时 agent 会开在终端里协助。本清单只列「测什么 + 预期」，逐条勾。
> 夹具：`& $m show "$PWD\docs\test-fixtures\v0.3.3-smoke-test.md"`(一次测 A/B/表格)。
> `[x]` = agent 已用真实 dev/CDP/CLI 验证；`[ ]` = 仍需用户肉眼或会启动外部程序。

## 准备
- [x] `npm run dev` 起来,窗口正常(renderer console 0 error)
- [x] `$m = "<repo>\src\skills\show-in-marina\marina.cmd"`;`& $m ping` → online
- [x] 开一个本地 PowerShell session(非 SSH)

## Feature F · 终端路径可点链接
- [x] 带斜杠路径(`src/x.ts:42`)鼠标悬停有下划线 → 点击在「已打开」打开
- [x] 带 `:行号` 的打开后**滚到那行**(居中,不高亮)
- [x] 属性访问(`.length`/`.map`)、裸文件名(`README.md`)**不**变蓝
- [x] 选中终端文本 → 右键有「在面板打开」→ 选中即试(裸名也认)
- [x] 不存在的路径点击 → toast「文件不存在」(不弹 modal)
- [ ] SSH session:路径不变蓝、右键没该项

## Feature E.3 · 色条动画
- [x] idle = 左侧细竖条(info 色)
- [x] 跑命令(active):细条 ~1s 变宽覆盖整行背景 + 标题文字同步反色
- [x] active 稳态:背景 opacity 循环脉冲(~2.6s)
- [x] 命令结束(active→idle):背景**逆收缩**回细条(同曲线,非瞬切)
- [x] exited:灰细条 + 整行 dim + 退出图标(✓/✗)

## Feature A · Gallery(```gallery)
- [x] 代码块渲染成幻灯片(主图+缩略图条+箭头+`N/总数`指示器)
- [ ] 网络图首次加载(本机访问 picsum 超时；此前已验证正常)
- [x] 点箭头/缩略图/键盘←→切换正常
- [x] 不存在的本地图 → 失败占位+重试按钮+指示器 `⚠N`
- [ ] 点主图 → 系统图片查看器打开(用户确认，agent 不自动拉起外部程序)

## Feature B · Markdown 链接
- [x] 相对路径链接(`../../README.md`)→ 「已打开」新 tab 只读
- [x] 不存在的链接 → toast 报错,面板不变
- [x] `#锚点` → 文档内滚动(不新开)
- [x] Markdown 本地链接打开及普通文件 tab 切换：loading 首帧直接使用目标 viewer 背景，不闪 dock 主题色
- [ ] `http(s)`/`mailto` → 系统(用户确认，agent 不自动拉起外部程序)

## Feature C · Diff 打开源文件
- [x] Git 面板点改动文件进 Diff → 左上「打开源文件」按钮 → 点了在「已打开」显示完整源
- [x] “已打开”Diff 页签的文件 icon 带 D 角标；普通文件无角标，icon 均保持 14×14px

## Feature G · 命令面板
- [x] `marina run "echo hello"` → 自动切「命令」tab + 显示输出
- [x] 同命令再推 → 去重不新增 tab(重跑)
- [x] 多 tab 并存;× 关 tab;↻ 手动重跑
- [x] 「后台 5s」策略:切走仍在跑(切回输出更新);「仅前台」切走停
- [x] 不存在的命令 → tab 显示 error
- [ ] SSH session:无「命令」tab

## Feature D · workspace 绑定/持久化
- [x] `workspace bind --name X` → 命名+pin
- [x] `workspace new` → 「已打开」**清空**(切到新空 workspace)
- [x] `workspace bind --name X`(切回)→ 之前文件 tab **恢复**
- [x] `workspace list` / `unpin` 正常
- [x] (强测)退出重启 → bind 同名 → 文件恢复

## Feature E.1+E.2 · 分组+拖拽
- [x] 右键收藏/临时/最近或已有分组 → 新建分组 modal；已有分组菜单含重命名/删除；header 折叠/展开
- [x] 拖 session 排序(同一 path 内)→ 松手顺序变
- [x] 拖收藏路径排序(同组/跨组)→ 松手归属/顺序变；空组和折叠组头也可接收（真实 Electron 指针验证）
- [x] 有/无终端的路径名称 x 坐标一致；session 模板 icon 按一级树深度缩进
- [x] session 状态色条在未分组/分组内都紧贴侧栏 x=0；active 满底时模板 icon 与名称同步反色

## 修复项
- [x] T13:终端 URL(`https://...`)变蓝可点
- [ ] T13:点击 URL → 打开浏览器(用户确认，agent 不自动拉起外部程序)
- [x] 表格内长链接不撑爆表格(自动断行)
- [ ] SCROLL-2:vim/Claude Code 等 alt-screen TUI 退出后滚动条**不跳顶**(用户肉眼确认)

## T12 · 截图(可选)
- [x] `marina screenshot` → 929,635-byte PNG，3842×2078

## 发版前总检(自动化)
- [x] `package.json` / `package-lock.json` / CHANGELOG 同步为 `0.3.3-preview`
- [x] `lint` 0 / 72 test files、1201 tests passed / `electron-vite build` 三 bundle 通过
- [x] `lint:css` 本轮 0 新错误：全文件既存基线 84 → 当前 83（完整命令仍非零，不在本轮扩散修复）
- [x] Portable：`release/0.3.3-preview/Marina-Portable-0.3.3-preview-x64.exe`（83.2 MB）
- [x] `verify-artifacts --strict`：3/3 `.node` 为 Windows、0 错误、0 警告；SHA256 `27c112baa3b98031062aed8451f8aa3d97a389bf799a2740e7f79fce4bea0d27`
- [x] 隔离 profile 启动冒烟：版本/IPC/buildType=`portable` 正确、renderer console 0 error；打包 CSS 几何验证分组内 session/bar x=0、icon x=32；profile 已清理

## Ubuntu 26.04 部署（FEX，2026-08-04）
- [x] FEX 原生构建（无 Docker）：`~/.cache/marina-build/de42bf7` 浅克隆 `feat/v0.3.3`
- [x] electron-builder 首次 `rebuild-node-modules` 在 FEX 挂起（0% CPU 无网络）→ `--config.npmRebuild=false` 跳过冗余重建（npm ci 的 install-app-deps 已按 Electron 31.7.7 编好 pty.node）
- [x] 产物：`Marina-0.3.3-preview-amd64.deb`（79.7MB，SHA256 `bb5e1f54...`）+ `Marina-0.3.3-preview-x86_64.AppImage`（117.3MB，SHA256 `89817a37...`）；SUID 修复后重打 deb：SHA256 `de50094284a26e6f7aeb17878979eaf02617d9af8c82d4e2770acf2428c96495`
- [x] deb control 依赖含 `libgtk-3-0t64 | libgtk-3-0`、`libatspi2.0-0t64 | libatspi2.0-0` 别名（26.04 t64 迁移）
- [x] unpacked 仅 1 个 `.node`（ELF x86-64 pty.node），无跨平台污染
- [x] 真实 GUI 冒烟（Xwayland :0 + mutter Xauthority）：应用启动、renderer ready、IPC 返回 `0.3.3-preview`
- [x] 真实 PTY spawn 成功（Bash pid=68474），`cmd:app:quit` 干净退出；临时 profile/解压目录已清理
- [x] 部署路径：`~/marina-0.3.3-preview/`（deb + AppImage + latest-linux.yml，188MB）
- [x] 安装完成：`sudo apt install -f` 补装 `libxss1`（唯一缺失依赖，源内有候选）后 dpkg 配置完成，状态 `ii 0.3.3~preview`；`/usr/bin/marina → /opt/Marina/marina`，`x-terminal-emulator` alternatives 注册成功
- [x] 安装版启动冒烟通过：`marina` 启动、IPC 返回 `0.3.3-preview`（buildType=installed）、干净退出；临时 profile 已清理
- [x] **坑：裸跑 `marina` 需 SUID sandbox**——electron-builder 的 chrome-sandbox 默认 0755，裸跑即 FATAL `setuid_sandbox_host`。已修 `deb-postinst.sh` 安装时 `chown root:root + chmod 4755`（commit `543a120`）；重打 deb 覆盖重装后 `/opt/Marina/chrome-sandbox` 为 `-rwsr-xr-x`，**裸跑验证通过**（无 --no-sandbox，IPC 0.3.3-preview）。教训：冒烟必须覆盖裸跑路径，不能只走 --no-sandbox
- [x] **坑：Ubuntu 26.04 裸跑 AppImage 必然失败**——仓库已彻底移除 libfuse2（只有 fuse3），`dlopen libfuse.so.2` 报错，apt 无包可装。须用 `APPIMAGE_EXTRACT_AND_RUN=1`（已验证可用）或改装 deb。冒烟时只走了 FUSE-less 解压路径，未覆盖裸跑路径，故未提前发现。
- [ ] `typecheck`：本次修改文件 0 新错误；仓库仍有既存 CommandPanel/Gallery/Sidebar 严格错误

## Ubuntu 26.04 第二轮部署（FEX，2026-08-04，侧栏三修复 #47/#48/#49）
- [x] 增量 bundle 同步 9 个 commit 到 `~/.cache/marina-build/de42bf7`（`543a120..8205154`）；package.json/lock 零变化，复用 FEX node_modules
- [x] `npm run build:linux`：electron-vite 3.69s + electron-builder；rpm 因缺 `rpmbuild` 失败（上轮同；本轮只交付 deb+AppImage）
- [x] AppImage 单独重跑刷新 `latest-linux.yml`：sha512（base64）与 AppImage 实测一致（`j4itouvx...`）
- [x] deb 重打对齐 AppImage 同源 `out/`：SHA256 `1aeff00d82dab01dc45d0e39ec6c6a5e1191b5b44c53d192b5305c752d79fd39`；AppImage SHA256 `92d3dd37b97ff59a732304d747b580bda080d92127cc1f7a50154f1c5c2893e4`；deb 内 pty.node mtime 与构建时间一致
- [x] 部署 `~/marina-0.3.3-preview/`：deb + AppImage + latest-linux.yml（188MB）
- [x] `sudo apt install -y ./Marina-...deb` 覆盖安装成功（`0.3.3~preview`，状态 ii）；`/opt/Marina/chrome-sandbox` 仍 `-rwsr-xr-x`（SUID 修复保留）
- [x] **裸跑冒烟通过（无 --no-sandbox）**：Xwayland :0 + mutter Xauthority；`cmd:app:get-protocol-version` → buildVersion=`0.3.3-preview`、buildType=`installed`；真实 session 创建后 state=idle（PTY 存活）；`cmd:app:quit` 干净退出；profile/探针已清理
- [x] **坑：FEX 上 9236/9229 端口被 GitLab 占用（gitaly/gitlab-workhorse）**——CDP `Cannot start http server for devtools`，换 9337 后正常。FEX 选调试端口前先 `ss -tln` 查占用
- [x] `git push fork feat/v0.3.3`：`c74cd37..8205154`（9 commit）
- [ ] 远程窗口验证（Windows 客户端→FEX daemon）待 FEX 上运行中的旧实例重启加载新版后进行
