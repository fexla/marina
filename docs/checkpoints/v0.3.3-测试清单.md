# v0.3.3 测试清单

> 测试时 agent 会开在终端里协助。本清单只列「测什么 + 预期」，逐条勾。
> 夹具：`& $m show "$PWD\docs\test-fixtures\v0.3.3-smoke-test.md"`(一次测 A/B/表格)。

## 准备
- [ ] `npm run dev` 起来,窗口正常
- [ ] `$m = "<repo>\src\skills\show-in-marina\marina.cmd"`;`& $m ping` → online
- [ ] 开一个本地 PowerShell session(非 SSH)

## Feature F · 终端路径可点链接
- [ ] 带斜杠路径(`src/x.ts:42`)鼠标悬停有下划线 → 点击在「已打开」打开
- [ ] 带 `:行号` 的打开后**滚到那行**(居中,不高亮)
- [ ] 属性访问(`.length`/`.map`)、裸文件名(`README.md`)**不**变蓝
- [ ] 选中终端文本 → 右键有「在面板打开」→ 选中即试(裸名也认)
- [ ] 不存在的路径点击 → toast「文件不存在」(不弹 modal)
- [ ] SSH session:路径不变蓝、右键没该项

## Feature E.3 · 色条动画
- [ ] idle = 左侧细竖条(info 色)
- [ ] 跑命令(active):细条 ~1s 变宽覆盖整行背景 + 标题文字同步反色
- [ ] active 稳态:背景 opacity 循环脉冲(~2.6s)
- [ ] 命令结束(active→idle):背景**逆收缩**回细条(同曲线,非瞬切)
- [ ] exited:灰细条 + 整行 dim + 退出图标(✓/✗)

## Feature A · Gallery(```gallery)
- [ ] 代码块渲染成幻灯片(主图+缩略图条+箭头+`N/总数`指示器)
- [ ] 网络图首次加载(骨架→图),缩略图当前项青绿高亮
- [ ] 点箭头/缩略图/键盘←→切换正常
- [ ] 不存在的本地图 → 失败占位+重试按钮+指示器 `⚠N`
- [ ] 点主图 → 系统图片查看器打开

## Feature B · Markdown 链接
- [ ] 相对路径链接(`../README.md`)→ 「已打开」新 tab 只读
- [ ] 不存在的链接 → toast 报错,面板不变
- [ ] `http(s)`/`mailto` → 系统;`#锚点` → 文档内滚动(不新开)

## Feature C · Diff 打开源文件
- [ ] Git 面板点改动文件进 Diff → 左上「打开源文件」按钮 → 点了在「已打开」显示完整源

## Feature G · 命令面板
- [ ] `marina run "echo hello"` → 自动切「命令」tab + 显示输出
- [ ] 同命令再推 → 去重不新增 tab(重跑)
- [ ] 多 tab 并存;× 关 tab;↻ 手动重跑
- [ ] 「后台 5s」策略:切走仍在跑(切回输出更新);「仅前台」切走停
- [ ] 不存在的命令 → tab 显示 error
- [ ] SSH session:无「命令」tab

## Feature D · workspace 绑定/持久化
- [ ] `workspace bind --name X` → 命名+pin
- [ ] `workspace new` → 「已打开」**清空**(切到新空 workspace)
- [ ] `workspace bind --name X`(切回)→ 之前文件 tab **恢复**
- [ ] `workspace list` / `unpin` 正常
- [ ] (强测)退出重启 → bind 同名 → 文件恢复

## Feature E.1+E.2 · 分组+拖拽
- [ ] 分组 header 折叠/展开
- [ ] 拖 session 排序(同组/跨组)→ 松手顺序变

## 修复项
- [ ] T13:终端 URL(`https://...`)变蓝可点 → 点开浏览器
- [ ] 表格内长链接不撑爆表格(自动断行)
- [ ] SCROLL-2:vim/Claude Code 等 alt-screen TUI 退出后滚动条**不跳顶**

## T12 · 截图(可选)
- [ ] `marina screenshot` → 存 PNG(路径打印)

## 发版前总检(自动化)
- [ ] `npm run typecheck` / `lint` / `test`(1187)/ `build` 无回归
